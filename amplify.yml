version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd webapp
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: webapp/.next
        files:
          - '**/*'
      cache:
        paths:
          - webapp/node_modules/**/*
